# VMI-Process-Real-Detail-Infor
The project is based on VMI to detect the VM safty.
client use python to get the VM inside state info.
host use LibVMI to get process/files/socket/syscall info from outside. Developing by C.

